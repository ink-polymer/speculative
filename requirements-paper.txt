# Install a matching CUDA PyTorch wheel separately; do not overwrite a server's driver.
transformers==4.57.1
huggingface-hub==0.36.0
datasets==3.6.0
accelerate==1.10.1
safetensors==0.6.2
numpy==2.2.6
tqdm==4.67.1
pytest==8.4.2
packaging==25.0
loguru==0.7.3
ninja>=1.11
typing-extensions>=4.10
# Install a flash-attn wheel matching the server's PyTorch/CUDA build separately.
